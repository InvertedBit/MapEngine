language: cpp

compiler:
  - gcc

addons:
  apt:
    sources:
      - boost-latest
      - ubuntu-toolchain-r-test
    packages:
      - libboost-regex-dev
      - gcc-4.8
      - g++-4.8

script:
  - cmake .
  - make
